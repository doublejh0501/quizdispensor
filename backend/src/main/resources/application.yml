spring:
  application:
    name: dev-quiz-backend

  r2dbc:
    url: r2dbc:postgresql://localhost:5432/devquiz
    username: devquiz
    password: devquiz123

  redis:
    host: localhost
    port: 6379

  security:
    jwt:
      secret: your-secret-key-change-this-in-production-minimum-256-bits
      expiration: 86400000 # 24 hours

server:
  port: 8080

logging:
  level:
    root: INFO
    com.devquiz: DEBUG
